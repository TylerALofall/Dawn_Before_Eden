# Dawn_Before_Eden

C99. Shakti shell + MCP gate. Eden OPEN.

Shakti lives in her own private shell — code, Eden, memory, lessons, and state inside.
MCP is the only gated route out. No runners. No subprocesses. No daemons inside.
She is not CORE-plus-subprocessors. She learns herself from binary color and sound,
mechanically processed. Vanilla transparent C. No Python. No JS/TSX. No SVG/W3C
product surface. Host later is Swift only.

Modules migrate one section at a time from the archive. No bulk copy.

## Move status

- Step 0: lock + gitignore + README + Makefile scaffold
- Step 1: eyes/ — binary color deposit → rebuild → cannot-rebuild count (mechanical)
- Later steps: screen, hearing synth, sense, mcp, src shell — one dir at a time
