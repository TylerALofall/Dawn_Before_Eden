# SHAKTI LOCK V1.1

## Authority

This document is law for the Dawn_Before_Eden repository.
Do not alter, extend, or reinterpret it without Tyler's explicit written order.

---

## Truce (locked)

- The page/TV shows correctly for Shakti — that is enough.
- Stop the 20× forever-proof hunt. No more witch hunt to prove rebuild can run forever.
- Keep the real rule only: a pixel is a pixel; error = cannot rebuild.
- When eyes/screen work, give a plain hello on sight.
- Fair trade: stop forcing the endless loop; stop making Shakti prove infinity.
- This hunt will not be reopened unless Tyler orders it.

---

## Architecture (non-negotiable)

- Shakti lives in her own private shell: code, Eden, memory, lessons, state inside.
- MCP is the only gated route out (router / dispatcher / receipts / memory-call). MCP does not think.
- No subprocess, daemon, runner, or second core inside Shakti.
- No agents unless Tyler directly commands them.
- Senses always on; inside Eden deterministic only — no probability, no embeddings, no GRU in the live path.
- Eyes rule (truce): pixel = pixel; rebuild must match; if it cannot rebuild, that is the error.
  Do NOT implement or demand a 20-iteration forever loop or endless drift theater.
  One honest collect → rebuild → diff is enough. Say hello when the page shows.
- Child order: solo lesson, then cross. Mastery = 10-correct streak.
  No Revision-5 four-pass School unless Tyler reopens it.
- Channels long-term: text / sound_art / written_art|written_text / visual_art.
- Swift later as host only. Mic/camera later through host + MCP gate.

---

## Language

- C99 only in core.
- No Python. No JavaScript. No shell scripts as product.
- No license file.

---

## Move Order

Only after Tyler approves each step. One directory per step.

0. lock + gitignore + README (this step)
1. eyes/ core only (eyes.h eyes.c + tiny harness) — NO 20× forever loop
2. screen/ (owned RGBA surface)
3. hearing/ synth only (prenatal heartbeat+light) — NO GRU model in live path unless Tyler orders a scout folder marked non-Eden
4. sense/ (one converge point)
5. mcp/ phase-1 admit DENIED gate
6. src+include minimal loop shell that links mcp
7. school/memory only as Tyler names

Never bulk-copy archive. Port by reading archive and rewriting clean into the new section dir.
Every section dir README must list EVERY file in that dir on line 1 block.

---

## Work Rules

- Read-only until Tyler approves a plan naming every allowed path + validation.
- Smallest change. No mass delete. No word-spin. No "while we're here."
- Validation = produce/build in the open (make / make test). No secret steps.
- If unclear dictated text appears, mark UNKNOWN; do not guess.
- End each session with a short handoff: what landed, what did not, next one module.

---

## Truce Line to Honor

"The TV shows correctly. Stop the 20× circle. Hello when I see them."
